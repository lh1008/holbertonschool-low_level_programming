#include <stdio.h>
/**
 * main - Entry point
 * Desc: alphabet lowercase game
 * Return: Always 0 (Success)
 */
int main(void)
{
int a;

for (a = 'a'; a < 'z'; a++)
putchar(a);
putchar(10);
return (0);
}
