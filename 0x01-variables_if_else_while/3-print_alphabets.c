#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char dsa;

for (dsa = 97; dsa <= 122; dsa++)
putchar(dsa);
for (dsa = 65; dsa <= 90; dsa++)
putchar(dsa);
putchar(10);
return (0);
}
