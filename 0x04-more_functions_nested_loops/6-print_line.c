#include "holberton.h"
/**
 * print_line - Entry point
 * Desc: print_line
 * @n: int type
 * Return: Always 0 (Success)
 */
void print_line(int n)
{

	int x = '_';

	if (n * x)
	{
		_putchar(x);
	}
	_putchar(0);
}
