#include "holberton.h"

/**
 * _atoi - Convert a string to an integer.
 * @s: input string
 * Return: the first integer content in the string
 */

int _atoi(char *s)
{
	_puts("Function pending implementation\n");
	_puts(s);

	return (0);
}
