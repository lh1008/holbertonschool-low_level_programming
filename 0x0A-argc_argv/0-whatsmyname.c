#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Desc: main function prints
 * @argc: int type
 * @argv: char type
 * Return: Print files name
 **/
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	(void)argc;

	return (0);
}
